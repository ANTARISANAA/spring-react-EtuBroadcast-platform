const scope = 'utils.validation.rulesMessages';

export const rulesMessages = {
  required: `${scope}.required`,
  maxLength: `${scope}.maxLength`,
  minLength: `${scope}.minLength`,
  email: `${scope}.email`,
  phoneNumber: `${scope}.phoneNumber`,
  numericInput: `${scope}.numeric`,
};
