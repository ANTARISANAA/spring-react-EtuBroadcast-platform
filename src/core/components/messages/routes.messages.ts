const scope = 'core.messages.routes';
export const routesMessages = Object.freeze({
  // This scope is only for global messages
  home: `${scope}.home`,
  students: `${scope}.students`,
    profile: `${scope}.profile`,
    login: `${scope}.login`,
    register: `${scope}.register`,
});
