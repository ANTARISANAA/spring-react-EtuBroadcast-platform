const scope = 'errorBoundary';

export const messages = Object.freeze({
  retryLater: `${scope}.retryLater`,
  retry: `${scope}.retry`,
  somethingWrong: `${scope}.somethingWrong`,
  unknownError: `${scope}.unknownError`,
  copyError: `${scope}.copyError`,
  stackTrace: `${scope}.stackTrace`,
  accessDeniedDescription: `${scope}.accessDeniedDescription`,
  returnHome: `${scope}.returnHome`,
  unexpected: `${scope}.unexpected`,
});
